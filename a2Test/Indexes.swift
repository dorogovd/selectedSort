import Foundation

class Indexes {
    var smallestIndex: Int
    var comparedIndex: Int
    
    init(smallestIndex: Int, comparedIndex: Int) {
        self.smallestIndex = smallestIndex
        self.comparedIndex = comparedIndex
    }
}
